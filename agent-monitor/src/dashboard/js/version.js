/* version.js — single source of truth for the dashboard version. */
window.VERSION = '4.7';
window.PRODUCT_NAME = 'gmux agent display';
window.PRODUCT_SHORT = 'GAD';
window.VERSION_DATE = '2026-05-13';
window.VERSION_NOTES = 'v4.7 / gmux-system v3.6.4: contents tree restored with "+/−" expand control per folder; full absolute file paths everywhere (no more ~/ collapse); detail panel auto-refreshes every 5s while open so live activity is always visible; historical (no recent ops) files render dim with dashed underline; data layer no longer Math.max-preserves file touch counters in Tauri mode — backend window arithmetic is now trusted.';
